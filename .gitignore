node_modules
.turbo
/.env
.vercel
.env*.local
prism.wasm
pglite.data
pglite.wasm
.cursor/mcp.json
dist/
benchmark-workspace/
coverage/
.serena/

# Claude local settings
.claude/settings.local.json

# Claude per-session work state — machine-local, never shared
.claude/session-state/

# Lefthook local config
lefthook-local.yml

# Browser-smoke scratch dirs (built ERD output, per-release)
_test/

# Hosts, paths and anything else this public repo must not carry
_credential/

# Local byproducts and anything that knows how to reach a host (see demo/README.md).
_workspace/
