# Discussions are not enabled on this repository, so there are no contact links —
# everything goes through issues. Blank issues stay enabled because the three
# templates do not cover every case (a question, a packaging problem, an idea).
blank_issues_enabled: true
